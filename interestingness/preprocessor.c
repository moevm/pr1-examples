// Use gcc -E to see more
#include <stdio.h>
#define N 10
#define M N + N
#define ABS(x) -x
#define HELLO "Hola amigos"
#define LETSGO main()
#define BACKBACKBACK return

int LETSGO{

	printf(HELLO": %d\n", -N + ABS(M - N));

	BACKBACKBACK 0;
}
